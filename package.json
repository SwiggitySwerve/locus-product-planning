{
  "name": "org-skills",
  "version": "1.0.0",
  "description": "Organization-level AI skills framework with Initiative Flow system",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "cli": "npx tsx openspec/bin/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "glob": "^10.3.10",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0",
    "yaml": "^2.3.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
